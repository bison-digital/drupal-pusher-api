pusher_api.authentication:
  path: '/admin/pusher-api/authentication'
  defaults:
    _controller: '\Drupal\pusher_api\Controller\AuthenticationController::authenticate'
    _title: 'Authenticate sockets with Pusher'
  requirements:
    _access: 'TRUE'
